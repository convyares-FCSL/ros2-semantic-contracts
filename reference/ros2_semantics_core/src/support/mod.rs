//! Support utilities (non-semantic helpers).
//!
//! This module is intentionally “support-level”: utilities, guards, gating helpers, etc.
//! It must not encode core action/lifecycle semantics.

pub mod gate;

// Optional convenience re-export:
// pub use gate::*;
